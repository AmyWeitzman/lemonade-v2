import { Box, Typography } from '@mui/material';

export default function TransportationPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">🍋 You Won't Get A 🍋</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Transportation — coming soon.</Typography>
    </Box>
  );
}
