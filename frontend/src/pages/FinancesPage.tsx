import { Box, Typography } from '@mui/material';

export default function FinancesPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">🌾 Harvest</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Finances — coming soon.</Typography>
    </Box>
  );
}
