import { Box, Typography } from '@mui/material';

export default function JobsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">🌱 Seeds to Trees</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Jobs — coming soon.</Typography>
    </Box>
  );
}
