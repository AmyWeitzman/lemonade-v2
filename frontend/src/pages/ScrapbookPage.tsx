import { Box, Typography } from '@mui/material';

export default function ScrapbookPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">📖 Life's Lemons</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Scrapbook — coming soon.</Typography>
    </Box>
  );
}
