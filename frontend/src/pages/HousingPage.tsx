import { Box, Typography } from '@mui/material';

export default function HousingPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">🏠 Home Sour Home</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Housing — coming soon.</Typography>
    </Box>
  );
}
