import { Box, Typography } from '@mui/material';

export default function ActionsPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h5">🍋 Squeeze the Day</Typography>
      <Typography color="text.secondary" sx={{ mt: 1 }}>Actions — coming soon.</Typography>
    </Box>
  );
}
